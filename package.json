{
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "npm run build && concurrently \"cd backend && pipenv run fastapi dev main.py\n\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "@google/generative-ai": "^0.12.0",
    "bootstrap": "^5.3.3",
    "react-bootstrap": "^2.10.2",
    "react-router-dom": "^6.23.1",
    "react-script": "^2.0.5"
  }
}
